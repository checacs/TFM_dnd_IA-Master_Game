// archivo de verificación temporal, seguro de borrar (usado para comprobar
// manualmente el fix de "faltan enemigos por colocar" en dm-turn.ts, ya que
// jest/ts-jest no resuelve en este sandbox). No se pudo eliminar por un bug
// de permisos del sandbox (mismo patrón que verify-tmp.ts en API_REST TFM) —
// se vacía en vez de borrarse. Puede eliminarse manualmente desde el
// explorador de archivos de Windows.
export {};
