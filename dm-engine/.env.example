DEEPSEEK_API_KEY=
# Verifica el id vigente en api-docs.deepseek.com (o la doc del proveedor que uses) antes de desplegar — cambia con frecuencia.
DEEPSEEK_MODEL=
# Opcional -- si no se define, usa https://api.deepseek.com. Cámbiala para usar
# cualquier otro proveedor compatible con la API de OpenAI (ej. Kimi K2/K3 de
# Moonshot: https://api.moonshot.ai/v1, con DEEPSEEK_MODEL=kimi-k3 y la key de
# platform.kimi.ai).
DEEPSEEK_BASE_URL=
# URL del servidor MCP de la API (docs/04-servidor-mcp.md).
MCP_SERVER_URL=http://localhost:3000/mcp
# Secreto compartido API <-> dm-engine (protege /mcp en la API y /turn en
# dm-engine). Tiene que ser EXACTAMENTE el mismo valor en los dos servicios.
# Genera uno largo y aleatorio, p.ej.: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Si se deja vacío, ambos endpoints quedan abiertos (solo para desarrollo local).
INTERNAL_API_KEY=
PORT=4000
